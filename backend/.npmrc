# Prevents npm from adding ^ or ~ to version numbers
save-exact = true

# Ensures consistent node_modules structure
package-lock = true

# Disable progress bars for cleaner Railway logs
progress = false
